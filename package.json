{
  "name": "waxios",
  "version": "0.0.4",
  "description": "A wrapper for axios http",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Allan Carvalho",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.12.0",
    "retry-promise": "^1.0.0"
  }
}
